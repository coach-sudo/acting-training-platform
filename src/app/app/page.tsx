import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";export default async function Page(){const s=await createClient(),{data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const{data}=await s.from("organization_memberships").select("organization_id").limit(1);if(!data?.length)redirect("/onboarding");return <main className="shell dashboard"><p className="eyebrow">Coach workspace</p><h1>Your training thread starts here.</h1><div className="card"><h2>No students yet</h2><p>Your studio starts here. Add an actor to begin tracking lessons, goals, assignments, and training history.</p><p className="muted">Student management arrives in Phase 2.</p></div></main>}
