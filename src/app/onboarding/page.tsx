import{onboard}from"../(auth)/actions";export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const{error}=await searchParams;return <main className="shell auth"><div className="card"><h1>Name your studio</h1>{error&&<p className="error">{error}</p>}<form action={onboard}><label>Your name<input name="displayName" required/></label><label>Studio name<input name="organizationName" required/></label><button>Create studio</button></form></div></main>}
