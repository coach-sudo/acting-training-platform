import{reset}from"../actions";export default async function Page({searchParams}:{searchParams:Promise<{error?:string;sent?:string}>}){const p=await searchParams;return <main className="shell auth"><div className="card"><h1>Reset password</h1>{p.sent?<p>Check your email.</p>:<form action={reset}>{p.error&&<p className="error">{p.error}</p>}<label>Email<input name="email" type="email" required/></label><button>Send reset link</button></form>}</div></main>}
