import{signup}from"../actions";export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const{error}=await searchParams;return <main className="shell auth"><div className="card"><h1>Create your studio</h1>{error&&<p className="error">{error}</p>}<form action={signup}><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" minLength={8} required/></label><button>Create account</button></form></div></main>}
