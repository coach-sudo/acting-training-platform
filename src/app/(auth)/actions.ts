"use server";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{credentialsSchema,emailSchema,onboardingSchema}from"@/lib/validation/auth";
const val=(f:FormData,n:string)=>String(f.get(n)??"");
export async function login(f:FormData){const p=credentialsSchema.safeParse({email:val(f,"email"),password:val(f,"password")});if(!p.success)redirect("/login?error=Invalid+credentials");const s=await createClient();if((await s.auth.signInWithPassword(p.data)).error)redirect("/login?error=Unable+to+log+in");redirect("/app")}
export async function signup(f:FormData){const p=credentialsSchema.safeParse({email:val(f,"email"),password:val(f,"password")});if(!p.success)redirect("/signup?error=Use+a+valid+email+and+8-character+password");const s=await createClient();if((await s.auth.signUp(p.data)).error)redirect("/signup?error=Unable+to+create+account");redirect("/onboarding")}
export async function reset(f:FormData){const p=emailSchema.safeParse(val(f,"email"));if(!p.success)redirect("/forgot-password?error=Invalid+email");const s=await createClient();await s.auth.resetPasswordForEmail(p.data);redirect("/forgot-password?sent=1")}
export async function onboard(f:FormData){const p=onboardingSchema.safeParse({displayName:val(f,"displayName"),organizationName:val(f,"organizationName")});if(!p.success)redirect("/onboarding?error=Enter+both+names");const s=await createClient();if((await s.rpc("create_coach_organization",{p_display_name:p.data.displayName,p_organization_name:p.data.organizationName})).error)redirect("/onboarding?error=Unable+to+create+studio");redirect("/app")}
export async function logout(){const s=await createClient();await s.auth.signOut();redirect("/")}
